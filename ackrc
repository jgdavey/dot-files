##
# You can manually search the vendor directory using:
#   ack --noignore-dir=vendor PATTERN
#
--ignore-dir=vendor

# Add HAML and Selenium to ruby list
--type-add=ruby=.haml,.rake,.rsel
--type-set=rdoc=.rdoc,.yardoc,.sdoc

# Sort found files lexically
--sort-files
