# Capistrano Global config file
begin
  require 'capistrano_colors'
rescue LoadError
  puts "Could not load the 'capistrano_colors' plugin."
  puts "You can install it with `gem install capistrano_colors`"
end

# Capistrano Colors custom config
=begin
capistrano_color_matchers = [
     { :match => /command finished/,       :color => :hide,      :prio => 10 },
     { :match => /executing command/,      :color => :blue,      :prio => 10, :attribute => :bright },
     { :match => /^transaction: commit$/,  :color => :magenta,   :prio => 10, :attribute => :blink },
     { :match => /git/,                    :color => :white,     :prio => 20, :attribute => :reverse },
   ]
colorize( capistrano_color_matchers )
=end


## Capistrano colors and options
=begin

:color can have the following values:

* :hide  (hides the row completely)
* :none
* :black
* :red
* :green
* :yellow
* :blue
* :magenta
* :cyan
* :white
=end
